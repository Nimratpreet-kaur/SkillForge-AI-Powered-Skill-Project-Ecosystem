/**
 * Project Service
 *
 * Handles project portfolio API calls.
 *
 * @owner Team Member 3 — Project Portfolio
 */

// Uncomment when implementing service functions:
// import api from './api';

// TODO: Implement project service functions

export const projectService = {
  // getAllProjects: async (params?: object) => {
  //   const response = await api.get('/projects', { params });
  //   return response.data;
  // },

  // getProjectById: async (projectId: string) => {
  //   const response = await api.get(`/projects/${projectId}`);
  //   return response.data;
  // },

  // createProject: async (data: object) => {
  //   const response = await api.post('/projects', data);
  //   return response.data;
  // },

  // updateProject: async (projectId: string, data: object) => {
  //   const response = await api.put(`/projects/${projectId}`, data);
  //   return response.data;
  // },

  // deleteProject: async (projectId: string) => {
  //   const response = await api.delete(`/projects/${projectId}`);
  //   return response.data;
  // },
};

export default projectService;
