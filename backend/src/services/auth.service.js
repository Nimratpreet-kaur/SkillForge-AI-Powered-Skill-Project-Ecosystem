/**
 * Auth Service
 * 
 * Business logic for authentication operations.
 * Separates auth logic from the controller for cleaner code and testability.
 * 
 * Planned functions:
 *   hashPassword(password)           — Hash password using bcrypt
 *   comparePassword(password, hash)  — Compare password against hash
 *   generateToken(userId)            — Generate JWT token
 *   verifyToken(token)               — Verify and decode JWT token
 * 
 * @owner Team Member 1 — Authentication
 */

// const bcrypt = require('bcryptjs');
// const jwt = require('jsonwebtoken');
// const { JWT_SECRET, JWT_EXPIRES_IN } = require('../config/env');

// TODO: Implement auth service functions

module.exports = {
  // hashPassword,
  // comparePassword,
  // generateToken,
  // verifyToken,
};
